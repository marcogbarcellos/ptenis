# HANDOFF — PTenis MVP (10/jun/2026)

**Para quem retoma:** este arquivo é a fonte da verdade do estado da execução. O plano completo (com TODO o código de cada task) está em `docs/superpowers/plans/2026-06-10-ptenis-mvp.md` — **leia o cabeçalho dele primeiro: as ERRATAS no topo são obrigatórias**. O spec de produto está em `docs/superpowers/specs/2026-06-10-ptenis-mvp-design.md`.

## 1. Estado atual

- **Branch:** `feat/mvp` (a `main` só tem os docs). NÃO commitar na main.
- **Tasks CONCLUÍDAS e aprovadas (spec + quality review, com fixes aplicados): T1 a T10 de 23.**
  - T1 scaffold (Next 16.2.9, Tailwind v4, shadcn base-nova/@base-ui, Vitest, Docker pg 5433)
  - T2 schema Prisma 7 completo + migration + seed + singleton
  - T3 senha/sessão/current-user/action-state + helpers de teste int
  - T4 questionário de nível · T5 contas + páginas auth · T6 casca (nav/PWA/tema)
  - T7 regras de placar · T8 serviço de convites · T9 serviços jogo/placar · T10 página Início (mural)
- **Último commit:** `432d765` (fix: type="submit" nos forms de auth). Histórico limpo, 1+ commit por task, todos com `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` (manter o padrão).
- **Working tree:** limpo após este commit de docs.
- **Gates atuais (todos verdes):** `npm run lint` (0 warnings) · `npm run typecheck` · `npm run test` (18 unit) · `npm run test:int` (22 int) · `npm run build`.
- **PRÓXIMA TASK: T11 (Página Jogar — amistosos), JÁ PARCIALMENTE ESCRITA.** Os 3 arquivos existem **não-commitados e não-verificados** em `src/app/(app)/jogar/` (`page.tsx`, `actions.ts`, `forms.tsx`): typecheck e lint passam, os 2 botões já têm `type="submit"`. FALTA: (a) conferir o conteúdo contra o plano Task 11; (b) checar a colisão de `id`s entre os dois forms (ambos usam `quando`/`local`/`formato`/`observacao` — se as Tabs do @base-ui mantiverem os dois painéis no DOM, prefixar os ids por form); (c) rodar os 5 gates; (d) verificação em browser (criar convite aberto e combinado — o redirect pro `/jogo/[id]` 404a até a T12, esperado; conferir que o jogo aparece no mural/próximos); (e) commit `feat: criação de amistoso aberto e combinado na aba Jogar`.

## 2. Como retomar (ambiente)

```bash
cd /Users/barsmike/workspace/personal/ptenis
git checkout feat/mvp
docker compose up -d        # Postgres 16 na porta 5433 (container ptenis-db-1)
npm i                       # se necessário
npm run lint && npm run typecheck && npm run test && npm run test:int && npm run build
```

- `.env` já existe localmente (NUNCA commitar). Bancos: `ptenis` (dev) e `ptenis_test` (int tests), ambos em `localhost:5433`.
- **Login de teste:** seed admin `marco.barcellos@juridico.ai` / `trocar123` (recriável com `npm run db:seed`). Código de convite: `PTENIS2026`.
- E-mails sem `RESEND_API_KEY` viram `console.log` (incluindo o HTML — o link de reset aparece no log).

## 3. Processo recomendado daqui pra frente (ECONOMIA DE TOKENS)

Até aqui o processo era 3 agentes por task (implementador + revisor de spec + revisor de qualidade). **Abandone isso.** Para o restante:

1. Implemente **diretamente na sessão**, task por task, transcrevendo o código do plano (ele é completo — não invente).
2. Após cada task: rode os 5 gates. Commit com a mensagem que o plano especifica.
3. **Verificação em browser SÓ nas tasks de UI com fluxo novo** (T12, T19, T21): suba `npm run dev`, faça o fluxo principal com Playwright ou manualmente, mate o servidor. Motivo: o pior bug do projeto (botões que não submetiam) só apareceu em browser real — gates automáticos não pegam.
4. Um auto-review rápido por task (reler o diff) basta. Guarde revisão pesada para o final (passo 6 abaixo).
5. Não re-litigue decisões: o spec e o plano foram aprovados pelo usuário.
6. **Ao terminar T23:** rode um code review geral do branch (ex.: `/code-review`), corrija o que for Critical/Important, e pergunte ao usuário como integrar (merge na main / PR) — skill `superpowers:finishing-a-development-branch`.

## 4. Tasks restantes (T11 → T23) e observações específicas

O texto/código completo de cada uma está no plano. Observações acumuladas das reviews (aplicar quando chegar lá):

- **T11 — Página Jogar (amistosos):** seguir o plano + erratas (`type="submit"` nos 2 botões de form; `buttonVariants()` no lugar de `Button asChild`). ATENÇÃO: os dois forms da página têm os mesmos `id`s nos campos (`quando`, `local`…). Se as Tabs do @base-ui mantiverem os dois no DOM, prefixe os ids por form (ex.: `quando-aberto` / `quando-comb`) mantendo os `htmlFor` casados. Redirect pós-criação vai pra `/jogo/[id]` que 404a até a T12 — esperado.
- **T12 — Página do jogo:** plano completo. `ConfirmButton` já é controlado (fecha sozinho no sucesso). Verificar em browser o ciclo: aceitar → lançar placar → confirmar na outra conta.
- **T13 — Jogadores e perfis:** plano completo (inclui `src/lib/stats.ts` com `historico` separado de `jogos`). `sairAction` já existe em `(auth)/actions.ts`.
- **T14/T15/T16 — puros (round-robin, classificação, playoffs/divisões):** transcrição direta + TDD. Baratos.
- **T17 — serviço de temporada:** o plano já contém a versão com `$transaction` opcional (DI). Estender `resolverPendencias` em `jogos.ts` para chamar `criarFinaisProntas` (sem ciclo de import — temporada.ts não importa jogos.ts).
- **T18 — serviço da liga:** transcrição direta.
- **T19 — página Temporada:** browser check. `searchParams` é Promise (Next 16).
- **T20 — liga na aba Jogar + propostas no jogo:** adicionar import de `Button`/`buttonVariants` que o snippet do plano omite. Observação herdada da review do T10: um jogo `proposto` recebido aparece em "Para você resolver" E em "Seus próximos jogos" — se ficar feio, filtre `proposto` dos próximos quando `proposedById !== user.id`.
- **T21 — admin temporadas/jogos:** browser check do ciclo completo (criar temporada → inscrições → divisões → liga → playoffs → encerrar). Herdado da review do T9: ao mexer em `adminDefinirPlacar`/`adminMarcarWO`, limpar `reportedById`/`reportedAt` velhos (1 linha cada). Dialog do admin: lembrar que `AlertDialogAction`/`DialogTrigger` não têm `asChild`.
- **T22 — admin usuários/config:** transcrição direta (controles client com selects/dialogs).
- **T23 — deploy Railway:** passos do plano + itens acumulados: (a) garantir `.env` no `.gitignore` (já está) e `postinstall: prisma generate` — ATENÇÃO: `prisma.config.ts` exige `DATABASE_URL` até pra generate; em máquina nova sem `.env` o `npm i` falha (documentar no README: copiar .env antes). Railway injeta env no build, então lá funciona. (b) Acrescentar checagem fail-fast de env no boot (APP_URL, SESSION_SECRET, DATABASE_URL) — sugestão: validar no `src/lib/db.ts` ou num `src/lib/env.ts` importado pelo layout raiz. (c) Resend free só entrega para o e-mail do dono até verificar domínio — sem `RESEND_API_KEY` em prod, reset de senha NÃO funciona; avisar o usuário no resumo final. (d) `gh repo create` + passos do dashboard Railway exigem o USUÁRIO — entregue o checklist pra ele em vez de tentar fazer.
- **Polimentos opcionais (só se sobrar fôlego, baixa prioridade):** JogoCard trunca o nome do adversário quando o do criador é longo (empilhar nomes); pino de teste do arredondamento em `nivel.test.ts`.

## 5. Armadilhas já descobertas (resumo das ERRATAS do plano — LEIA TODAS LÁ)

1. **Porta 5433** para o Postgres local (5432 estava ocupada). Tudo já configurado; não "corrigir" de volta.
2. **Prisma 7:** URL do datasource em `prisma.config.ts`; `PrismaClient` exige adapter (`PrismaPg`); `db push --force-reset` bloqueado para agentes — global-setup dos testes já usa `DROP SCHEMA + migrate deploy`; tipos/enums de `@prisma/client`; singleton `db`/`Db`/`Prisma` de `@/lib/db`; `score` Json usa `sets as Prisma.InputJsonValue` ao gravar e `Prisma.DbNull` ao limpar.
3. **shadcn @base-ui:** SEM `asChild` (usar `render` ou `buttonVariants()`); `Button` em form precisa de `type="submit"` explícito; `AlertDialogAction` não fecha o diálogo sozinho (ConfirmButton já trata); sonner com `theme="light"` fixo.
4. **Fontes:** classes de variável Geist ficam no `<html>` (NUNCA no body) — já quebrou 2x.
5. **Vitest não carrega `.env`:** `vitest.int.config.ts` já seta `APP_URL`; `linkJogo()`/`appUrl()` LANÇAM sem `APP_URL`.
6. **Next 16:** `params`/`searchParams` são Promise (`await`); `useActionState` de "react"; redirects fora do `runAction` (ele rethrowa via `unstable_rethrow`, mas o padrão do código é redirect fora).
7. **Convenções:** serviços recebem `db: Db` como 1º parâmetro; erros de negócio = `AppError` em pt-BR; actions = `requireUser`/`requireAdmin` + `runAction` + `revalidatePath("/", "layout")`.

## 6. Definição de pronto (por task e final)

- **Por task:** 5 gates verdes + commit no padrão + (UI nova → fluxo principal verificado em browser).
- **Final (pós-T23):** ciclo completo manual em produção (cadastro → convite → aceite → placar → confirmação → temporada de teste → playoffs → troféu), PWA instalável no celular, review geral do branch, e decisão de merge com o usuário.
